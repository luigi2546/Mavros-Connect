kind = "web"
previewPath = "/"
title = "Mavros Connect"
version = "1.0.0"
id = "artifacts/mavros-connect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24176

[services.development]
run = "pnpm --filter @workspace/mavros-connect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mavros-connect", "run", "build" ]
publicDir = "artifacts/mavros-connect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24176"
BASE_PATH = "/"
